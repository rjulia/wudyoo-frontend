import React from 'react'
import useStyles from './cart-category-style'
const CardCategory = (props) => {
  const styles = useStyles()
  return (
    <div className={styles.container}>
      
    </div>
  )
}

export default CardCategory
